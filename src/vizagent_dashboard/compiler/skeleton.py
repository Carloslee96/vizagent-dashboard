"""HTML skeleton — generate the complete HTML page with embedded ECharts."""

from __future__ import annotations


def build_html(
    chart_options: list[dict],
    kpi_options: list[dict],
    title: str,
    theme_tokens: dict,
) -> str:
    """Generate the complete self-contained HTML page."""
    raise NotImplementedError("Will be extracted from SaaS in Step 2")
